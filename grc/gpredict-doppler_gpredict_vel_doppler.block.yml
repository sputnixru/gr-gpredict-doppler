# auto-generated by grc.converter

id: gpredict_vel_doppler
label: Velocity-Based Doppler
category: '[GPredict]'

parameters:
-   id: gpredict_host
    label: Local Listening IP
    dtype: string
    default: '127.0.0.1'
    options: ['127.0.0.1', '0.0.0.0']
    option_labels: [localhost, All IPs]
-   id: gpredict_port
    label: Listening Port
    dtype: int
    default: '7360'
-   id: frequency
    label: Known Frequency
    dtype: float
    default: freq
-   id: velocity
    label: Velocity (m/s)
    dtype: float
    default: velocity
-   id: verbose
    label: Verbose
    dtype: bool
    default: 'False'
    options: ['False', 'True']
    option_labels: ['No', 'Yes']

inputs:
-   domain: message
    id: velocity
    optional: true

outputs:
-   domain: message
    id: frequency
    optional: true
-   domain: message
    id: freqshift
    optional: true

templates:
    imports: import gpredict
    make: gpredict.vel_doppler(${frequency},${velocity},${gpredict_host}, ${gpredict_port},
        ${verbose})

documentation: "Given a known frequency and a relative velocity (in m/s), this block\
    \ will calculate the doppler-shifted frequency and output it in two forms on the\
    \ message ports.  One output is the full frequency, the other is just the relative\
    \ shift.  Both are in Hz.\n\t\n\tNote: The velocity coordinate system is defined\
    \ as Negative = towards you, Positive = away."

file_format: 1
